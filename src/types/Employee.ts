// TypeScript interface to define the structure of an Employee object
export interface Employee {
    id: string;
    firstName: string;
    lastName: string;
    email: string;
    phone: string;
    gender: string;
    dob: string;
    joinedDate: string;
}
